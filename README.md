Terraform config file for AWS deployment. 

Following commands can and should be used for terraform: 
- terraform init - **** Use this to intitialize a newly pulled config from version control (git)


- terraform fmt - **** Use this to format terraform files